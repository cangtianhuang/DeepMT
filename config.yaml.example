# DeepMT 配置文件示例
# 复制此文件为 config.yaml 并填入您的配置

# LLM配置（必需）
llm:
  provider: "openai"  # openai, anthropic, local
  api_key: ""         # 请在此处填入您的API密钥，或设置环境变量 OPENAI_API_KEY
  model_base: ""      # 基础模型 ID
  model_max: ""       # 高级模型 ID（可选，留空则与 model_base 相同）
  url: ""             # LLM API 基础URL（使用自定义端点时填写）
  temperature: 0.2

# OCR 配置
ocr:
  enabled: false
  api_key: ""
  url: ""
